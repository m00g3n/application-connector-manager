# Default values for application-connectivity-validator-test.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  containerRegistry:
    path: "ghcr.io/m00g3n/application-connector-manager/test-images"

  images:
    validatorTest:
      name: "application-connectivity-validator-tests"
      version: "main"

  namespace: "test"
